# Absolute origin used for canonical URLs, sitemap, robots and share links.
# On Vercel this is inferred from VERCEL_PROJECT_PRODUCTION_URL, so setting it
# is optional — do set it once you have a custom domain.
NEXT_PUBLIC_SITE_URL=https://onemore.example.com
